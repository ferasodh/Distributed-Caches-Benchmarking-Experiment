::

:: List of default probes.
set BENCHMARK_DEFAULT_PROBES=ThroughputLatencyProbe

:: Packages where the specified benchmark is searched by reflection mechanism.
set BENCHMARK_PACKAGES=com.performance.hazelcast,org.yardstickframework

:: Probe point writer class name.
:: BENCHMARK_WRITER=

:: Comma-separated list of the hosts to run BenchmarkServers on. 2 nodes on local host are enabled by default.
set SERVER_HOSTS=localhost,localhost

:: Comma-separated list of the hosts to run BenchmarkDrivers on. 1 node on local host is enabled by default.
set DRIVER_HOSTS=localhost

:: Remote username.
:: set REMOTE_USER=

:: Run configuration which contains all benchmarks.
:: Note that each benchmark is set to run for 300 seconds (5 mins) with warm-up set to 60 seconds (1 minute).
set CONFIGS=^
--localBind 127.0.0.1 --port 1234 -d 30 -t 1 -dn HazelcastDriverBenchmark -sn HazelcastServer -ds haytham-salhi-description_2,^
--localBind 127.0.0.1 --port 1234 -d 30 -t 2 -dn HazelcastDriverBenchmark -sn HazelcastServer -ds haytham-salhi-description_4